import { LIFECYCLE_POLICY } from './policy.js';

/** Shared stale-verify days for Cognitive Health + Lifecycle (single source of truth). */
export const SHARED_STALE_VERIFY_DAYS = LIFECYCLE_POLICY.staleVerifyDays;
