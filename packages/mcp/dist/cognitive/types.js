/**
 * Contorium MCP Cognitive Overlay types.
 *
 * Mode naming (v2):
 *   A = Core Runtime — pure observation (DEFAULT)
 *   B = Cognitive Overlay — A + skills + presets + insights (B ⊃ A)
 */
export const COGNITIVE_MODE_SCHEMA_VERSION = 2;
